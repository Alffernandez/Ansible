ansible_tower_config:
  default:
    #
    # INVENTORIES
    #
    inventories:
      SCHRODERS_localhost_inventory: &SCHRODERS_localhost_inventory
        organization: SCHRODERS
        description: An inventory with just localhost in it
    #
    # INVENTORY SOURCES
    #
    # inventory_sources: 
  prd:
    inventories:
      SCHRODERS_localhost_inventory:
        <<: *SCHRODERS_localhost_inventory
    # inventory_sources:
  dev:
    inventories:
      SCHRODERS_localhost_inventory:
        <<: *SCHRODERS_localhost_inventory
    # inventory_sources:
  c_b:
    inventories:
      SCHRODERS_localhost_inventory:
        <<: *SCHRODERS_localhost_inventory
    # inventory_sources:
